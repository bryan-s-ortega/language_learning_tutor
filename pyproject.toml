[project]
name = "language-learning-tutor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pre-commit>=4.2.0",
    "ruff>=0.12.2",
    "rust-just>=1.41.0",
]
